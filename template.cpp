#include <bits/stdc++.h>
using namespace std;
#define llt long long int

int main() {
    llt t;
    cin >> t;

    while (t--) {
        llt n;
        cin >> n;
        vector<llt> A(n);

        for (llt i = 0; i < n; i++) {
            cin >> A[i];
        }

        cout << count << endl;
    }

    return 0;
}
